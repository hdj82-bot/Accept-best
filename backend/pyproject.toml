[tool.pytest.ini_options]
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "session"

[tool.coverage.run]
source = ["app"]
branch = true
omit = [
    "app/main.py",
    "app/__init__.py",
    "alembic/*",
    "tests/*",
]

[tool.coverage.report]
show_missing = true
skip_covered = false
