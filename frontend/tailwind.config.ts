/**
 * Tailwind v4 is CSS-first.
 * Dark mode is configured via `@custom-variant dark` in globals.css — not here.
 * This file exists as a reference; actual config lives in app/globals.css.
 *
 * Equivalent of `darkMode: 'class'` for v4:
 *   @custom-variant dark (&:where(.dark, .dark *));
 */
export default {};
